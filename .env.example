# Admin bearer token (for creating API keys)
ADMIN_BEARER_TOKEN=changeme123

# Transcription settings
MAX_AUDIO_SIZE_MB=256

# Whisper settings
# Using distil-large-v3 (Whisper 3 Turbo equivalent)
WHISPER_MODEL=distil-large-v3
WHISPER_COMPUTE_TYPE=float16

# Conductor integration (leave empty to run blurb standalone)
CONDUCTOR_URL=
BLURB_API_KEY=

# Max seconds before a transcription job is marked failed
JOB_TIMEOUT_SECONDS=3600
